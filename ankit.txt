(1)create db with the name of tops
->CREATE DATABASE tops

(2)create table with the name of student and predefined column is id and student_name
->CREATE TABLE student (id int AUTO_INCREAMENT ,student_name VARCHAR(50))

(3)after creation of table and add one more column student_fee
->ALTER TABLE student ADD student_fee INT 

(4)after creation of table and student_fees add one more column student_grade
->CREATE TABLE student (student_fees varchar(50),student_grade VARCHAR(50))

(5)change datatype of student_grade
->ALTER TABLE student MODIFY student_grade int

(6)remove column student_grade
->ALTER TABLE student DROP student_grad

(7)insert data in all column
->INSERT INTO `student`(`id`, `student_name`, `student_std`, `student_fee`) VALUES ('1','ankit','12','24000') 

(8)update data in any one column
->UPDATE student SET id='1' student_name='ankit',student_std='12',student_fee='24000' WHERE id='1'

(9)delete data from any column
->DELETE FROM student WHERE id=1

(10)select data according to grades
->SELECT student_name from student

(11)select data according to column wise
->select student_grade from student student id='1'